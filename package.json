{
  "name": "canvastodocs",
  "version": "1.0.0",
  "description": "Chrome extension that opens Canvas assignments directly in Google Docs",
  "main": "js/main.js",
  "scripts": {
    "build": "node scripts/build.js",
    "package": "npm run build && node scripts/package.js",
    "zip": "npm run build && bestzip DocLaunch-v$npm_package_version.zip dist/*",
    "clean": "rm -rf dist releases *.zip"
  },
  "keywords": [
    "chrome-extension",
    "canvas",
    "google-docs",
    "education"
  ],
  "author": "Liam Hardman",
  "license": "MIT",
  "devDependencies": {
    "bestzip": "^2.2.5",
    "fs-extra": "^11.3.5"
  }
}
